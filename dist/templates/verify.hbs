<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Email Verification</title>
    <style>
      :root { --primary-color: #2563eb; --primary-hover: #1d4ed8;
      --background-light: #f0f4ff; --text-dark: #1a1a1a; --text-muted: #6b7280;
      --accent-color: #3b82f6; } * { box-sizing: border-box; margin: 0; padding:
      0; } body { font-family: 'Inter', -apple-system, BlinkMacSystemFont,
      'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif; background-color:
      var(--background-light); line-height: 1.6; color: var(--text-dark); }
      .email-container { max-width: 540px; margin: 40px auto; background-color:
      white; border-radius: 16px; box-shadow: 0 12px 30px rgba(37, 99, 235,
      0.15); overflow: hidden; border: 1px solid rgba(37, 99, 235, 0.1); }
      .email-header { background: linear-gradient(135deg, #2563eb, #3b82f6);
      color: white; text-align: center; padding: 30px 20px; position: relative;
      overflow: hidden; } .header-pattern { position: absolute; top: 0; left: 0;
      right: 0; bottom: 0; opacity: 0.1; background-image:
      radial-gradient(circle at 20% 30%, rgba(255, 255, 255, 0.3) 0%,
      transparent 20%), radial-gradient(circle at 80% 70%, rgba(255, 255, 255,
      0.3) 0%, transparent 20%); } .logo { width: 160px; height: auto;
      margin-bottom: 15px; position: relative; z-index: 1; filter: drop-shadow(0
      4px 6px rgba(0, 0, 0, 0.1)); } .email-content { padding: 40px; text-align:
      center; background-image: linear-gradient(rgba(240, 244, 255, 0.3) 1px,
      transparent 1px), linear-gradient(90deg, rgba(240, 244, 255, 0.3) 1px,
      transparent 1px); background-size: 30px 30px; background-position: center
      center; } .verification-title { font-size: 28px; margin-bottom: 24px;
      color: var(--text-dark); font-weight: 700; } .verification-message {
      color: var(--text-muted); margin-bottom: 36px; font-size: 16px; max-width:
      400px; margin-left: auto; margin-right: auto; } .verify-button { display:
      inline-block; background: linear-gradient(135deg, #2563eb, #3b82f6);
      color: white !important; text-decoration: none; padding: 16px 38px;
      border-radius: 12px; font-weight: 600; font-size: 16px; transition: all
      0.3s ease; margin-bottom: 36px; box-shadow: 0 4px 14px rgba(37, 99, 235,
      0.3); border: none; position: relative; overflow: hidden; transform:
      translateY(0); } .verify-button:hover { transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(37, 99, 235, 0.4); } .verify-button:after {
      content: ''; position: absolute; top: 0; left: 0; right: 0; bottom: 0;
      background: linear-gradient(rgba(255, 255, 255, 0.1), rgba(255, 255, 255,
      0)); border-radius: 12px; } .divider { height: 1px; background:
      linear-gradient(to right, transparent, var(--background-light),
      transparent); margin: 20px 0; } .verification-icon { width: 80px; height:
      80px; margin: 0 auto 30px; background-color: var(--background-light);
      border-radius: 50%; display: flex; align-items: center; justify-content:
      center; } .verification-icon svg { width: 40px; height: 40px; fill:
      var(--primary-color); } .email-footer { background-color:
      var(--background-light); padding: 30px; text-align: center; border-top:
      1px solid rgba(107, 114, 128, 0.1); } .footer-note { color:
      var(--text-muted); font-size: 14px; margin-bottom: 12px; } .social-links {
      display: flex; justify-content: center; margin-top: 20px; gap: 16px; }
      .social-link { width: 36px; height: 36px; background-color: white;
      border-radius: 50%; display: flex; align-items: center; justify-content:
      center; color: var(--primary-color); text-decoration: none; box-shadow: 0
      2px 8px rgba(0, 0, 0, 0.05); transition: transform 0.3s ease; }
      .social-link:hover { transform: scale(1.1); } @media (max-width: 600px) {
      .email-container { margin: 20px; width: calc(100% - 40px); }
      .email-content { padding: 30px 20px; } }
    </style>
  </head>

  <body>
    <div class="email-container">
        <div class="email-header">
            <img src="https://res.cloudinary.com/dak07ttxh/image/upload/v1740768047/hp_wpqmi7.png" alt="TechLite Logo" class="logo">
        </div>

      <div class="email-content">

        <h1 class="verification-title">Verify Your Email</h1>
        <p class="verification-message">
          Thank you for joining TechLite! To complete your registration and
          activate your account, please verify your email address by clicking
          the button below.
        </p>
        <a href="{{link}}" class="verify-button">Verify Email Address</a>

        <div class="divider"></div>

        <p class="footer-note" style="margin-top: 20px;">
          Get ready to experience the best technology solutions!
        </p>
      </div>

      <div class="email-footer">
        <p class="footer-note">This link is valid for 1 hour</p>
        <p class="footer-note">If you did not create an account with TechLite,
          please ignore this email.</p>

      </div>
    </div>
  </body>

</html>